const fruits = ["Apple", "Orange", "Plum"];
fruits[2] = 'Pear';
console.log(fruits); // Note :- even after declaring as const the value inside the array can be altered but 
// it should not get reassigned





